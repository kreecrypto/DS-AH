{
  "name": "ds-ah-operating-repo",
  "private": true,
  "version": "2.0.0",
  "description": "Production Design Agent operating contract with full CREATE/MODIFY, REVIEW and QA skill pipelines, evidence gates and regression/fix loops.",
  "type": "module",
  "scripts": {
    "validate:registry": "node scripts/validate-registry.mjs",
    "validate:agent": "node scripts/validate-agent.mjs",
    "test:agent": "node scripts/run-agent-evals.mjs",
    "validate": "npm run validate:registry && npm run validate:agent && npm run test:agent && npm run test:fidelity",
    "test:fidelity": "node scripts/run-fidelity-evals.mjs"
  }
}
