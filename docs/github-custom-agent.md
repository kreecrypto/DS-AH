# GitHub Custom Agent — Deprecated

The previous project-level GitHub Custom Agent approach has been retired.

Current architecture:
- GitHub = Knowledge Base / operating contract
- ChatGPT = Design Agent runtime
- Figma MCP through ChatGPT = live execution

The file `.github/agents/design-agent.agent.md` is intentionally removed.

See `docs/runtime-architecture.md`.
