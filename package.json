{
  "name":"ds-ah-operating-repo",
  "private":true,
  "version":"2.1.0",
  "description":"Design Control Agent for governed Figma design execution, scope control, permission gates, QA, regression and evidence.",
  "type":"module",
  "scripts":{
    "validate:registry":"node scripts/validate-registry.mjs",
    "validate:agent":"node scripts/validate-agent.mjs",
    "test:agent":"node scripts/run-agent-evals.mjs",
    "test:fidelity":"node scripts/run-fidelity-evals.mjs",
    "test:control":"node scripts/run-control-evals.mjs",
    "validate":"npm run validate:registry && npm run validate:agent && npm run test:agent && npm run test:fidelity && npm run test:control"
  }
}